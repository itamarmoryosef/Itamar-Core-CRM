/**
 * Segment for a single client card. Shared chrome (header/sidebar) lives in
 * `app/admin/layout.tsx` (AdminShell). Tab UI and data are in `page.tsx`.
 */
export default function AdminClientDetailLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
